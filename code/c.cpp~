#include<iostream>
#include<cstdio>
#include<algorithm>
using namespace std;
const int N = 10000010;
int a[N];
int main() {
    int n,k;
    scanf("%d",&n);
    for(int i=1;i<=n;i++) scanf("%d",&a[i]);
    scanf("%d",&k);
    sort(a+1,a+1+n);
    printf("%d\n",a[k]);
    return 0;
}

